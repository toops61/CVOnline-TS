export interface albumsType {
    name: string;
    artist: string;
    year: number;
    content: string;
    cover: string;
    id:number | string;
}

export interface positionAlbumType {
    transform: string;
    zIndex: number;
    filter: string;
}